
function DishCard({ p, index }) {
 import { useEffect, useRef, useState } from "react";
import { gradText } from "../data/data";

export default function DishCard({ p, index }) {
   // paste code
}
}
